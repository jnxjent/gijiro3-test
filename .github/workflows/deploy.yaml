
name: Deploy Gijiro3 (Web & Function)

on:
  push:
    branches:
      - master

jobs:
  # ───────────────────────────────────────────────────────────
  # 1) Deploy Web App（ZipDeploy + clean:true で安全に差分反映）
  # ───────────────────────────────────────────────────────────
  webapp:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # （必要なら残す。ビルドが不要な場合は削除可）
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

      # Web用ZIPを作成（Function 等は除外）
      - name: Build web artifact (zip)
        run: |
          zip -r build.zip . \
            -x "ProcessAudioFunction/**" \
               ".git/**" ".github/**" ".venv/**" \
               "*/__pycache__/**" "*.log" "*.tar" "*.tar.gz" "*.zip" \
               "GIJIRO BACKUP/**" "GIJIRO_BACKUP/**" "backup/**" "BACKUP/**"

      # ZipDeploy（wwwrootへ展開）＋ clean:true で旧ファイル除去
      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          package: build.zip
          clean: true

  # ───────────────────────────────────────────────────────────
  # 2) Deploy Function App（従来どおり）
  # ───────────────────────────────────────────────────────────
  functionapp:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.12"

      - name: Install function requirements
        working-directory: ProcessAudioFunction
        run: |
          python -m venv .venv
          source .venv/bin/activate
          pip install -r requirements.txt

      name: Deploy to Azure Function
  uses: azure/functions-action@v1
  with:
    app-name: ${{ secrets.AZURE_FUNCTIONAPP_NAME }}
    package: ProcessAudioFunction
    publish-profile: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}

